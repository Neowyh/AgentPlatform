# Fault Zeroing Agent PoC Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a source-controlled iDeer PoC package for a fault-zeroing custom agent that can read uploaded evidence files, build a fault tree, assess bottom events, and generate Markdown deliverables.

**Architecture:** Keep iDeer runtime unchanged. Add a custom skill under `skills/custom/fault-zeroing/`, source-controlled agent/subagent configuration templates under `docs/fault-zeroing-agent/`, and sample evidence packages for repeatable manual validation. Runtime installation remains an explicit copy/bootstrap step because iDeer custom agents are stored per user under `backend/.ideer/users/{user_id}/agents/{agent_name}/`.

**Tech Stack:** Markdown, YAML, JSON templates, iDeer custom agents, iDeer custom skills, sandbox tools (`glob`, `grep`, `read_file`, `write_file`), built-in tools (`task`, `ask_clarification`, `present_files`).

**Important constraint:** Do not edit runtime symbols for this PoC. If implementation later changes Python or TypeScript functions/classes/methods, run GitNexus impact analysis before editing the symbol as required by `AGENTS.md`.

---

## File Structure

| File | Action | Responsibility |
| --- | --- | --- |
| `skills/custom/fault-zeroing/SKILL.md` | Create | Defines the agent workflow, evidence rules, fault-tree rules, bottom-event assessment rules, and report generation rules |
| `skills/custom/fault-zeroing/templates/zeroing_report.md` | Create | Markdown report template generated by the skill |
| `skills/custom/fault-zeroing/templates/fault_tree.json` | Create | JSON structure for fault tree and bottom events |
| `skills/custom/fault-zeroing/templates/bottom_event_assessment.md` | Create | Markdown table template for bottom-event assessment |
| `skills/custom/fault-zeroing/references/evidence_rules.md` | Create | Evidence grading and citation rules reused by the skill |
| `docs/fault-zeroing-agent/agent/config.yaml` | Create | Source-controlled custom agent config template |
| `docs/fault-zeroing-agent/agent/SOUL.md` | Create | Source-controlled SOUL template for `fault-zeroing` |
| `docs/fault-zeroing-agent/subagents.yaml` | Create | Config snippet for five custom subagents |
| `docs/fault-zeroing-agent/samples/case-01/` | Create | First sample evidence package |
| `docs/fault-zeroing-agent/samples/case-02/` | Create | Second sample evidence package |
| `docs/fault-zeroing-agent/验证记录.md` | Create | Manual PoC validation checklist and results table |

---

### Task 1: Create the `fault-zeroing` custom skill

**Files:**
- Create: `skills/custom/fault-zeroing/SKILL.md`
- Create: `skills/custom/fault-zeroing/references/evidence_rules.md`

- [ ] **Step 1: Create the skill directory**

Run:

```bash
mkdir -p skills/custom/fault-zeroing/references skills/custom/fault-zeroing/templates
```

Expected: command exits successfully and creates the directories.

- [ ] **Step 2: Write `SKILL.md`**

Create `skills/custom/fault-zeroing/SKILL.md` with this content:

```markdown
---
name: fault-zeroing
description: 基于文件资料完成归零排故、故障树构建、底事件评估和归零报告生成
allowed-tools:
  - glob
  - grep
  - read_file
  - write_file
  - present_files
  - task
  - ask_clarification
---

# 归零排故工作流

## 工作目标

基于用户上传的问题描述、日志、试验记录、设计资料、历史案例和报告模板，完成故障树构建、底事件评估、根因归因、验证计划和 Markdown 归零报告生成。

## 输入资料读取规则

1. 先确认顶事件、故障现象、发生条件、影响范围和已有资料清单。
2. 优先读取 `/mnt/user-data/uploads/`，其次读取 `/mnt/user-data/workspace/`。
3. 不确定文件位置时，先用 `glob` 获取目录结构，再用 `grep` 定位关键词。
4. 长文档按章节或行号范围读取，不一次性吞入无关内容。
5. 缺少关键输入时调用 `ask_clarification`，不得用假设替代资料。

## 证据处理规则

执行前读取 `references/evidence_rules.md`。每条关键结论必须包含证据来源，来源至少包含文件路径；能定位行号时写明行号或章节名。把内容区分为事实、推断、假设、待验证项。

## 故障树构建规则

1. 顶事件必须来自用户问题或资料原文。
2. 中间事件用于表达故障传播链、功能失效链或条件组合。
3. 底事件必须可验证，避免把笼统原因写成底事件。
4. 对每个底事件记录 `id`、`name`、`description`、`evidence`、`probability`、`confidence`、`status`。
5. 证据不足时，`probability` 填 `null`，`confidence` 填 `low`，`status` 填 `to_verify`。

## 底事件评估规则

使用“证据强度 + 机理一致性 + 反证情况 + 复现实验情况”形成概率判断。没有行业统计、历史频次或专家打分表时，不得给出精确数值概率，只给出高/中/低倾向和置信度。

## 子智能体协作规则

复杂资料包必须优先委托子智能体：

1. `evidence-reader`：抽取关键证据和来源。
2. `fault-tree-builder`：构建故障树草案。
3. `probability-assessor`：评估底事件概率、置信度和验证状态。
4. `root-cause-analyst`：形成根因归因和验证建议。
5. `report-reviewer`：检查章节完整性、证据闭环和待验证项。

子智能体失败时，记录失败原因，再由主智能体直接读取资料完成降级分析。

## 输出规则

输出文件写入 `/mnt/user-data/outputs/`：

1. `/mnt/user-data/outputs/fault_tree.json`
2. `/mnt/user-data/outputs/bottom_event_assessment.md`
3. `/mnt/user-data/outputs/zeroing_report.md`

写完后调用 `present_files` 展示三份文件。输出前检查报告是否包含问题概述、输入资料、故障现象、故障树分析、底事件评估、根因归因、验证计划、纠正措施、遗留风险和证据附录。
```

Expected: file exists and frontmatter contains the skill name `fault-zeroing`.

- [ ] **Step 3: Write evidence rules**

Create `skills/custom/fault-zeroing/references/evidence_rules.md` with this content:

```markdown
# 证据等级和引用规范

## 证据等级

| 等级 | 含义 | 可支持的结论 |
| --- | --- | --- |
| A | 原始日志、试验记录、检测数据、设计约束原文，且来源明确 | 可支持事实判断和高置信推断 |
| B | 专家记录、会议纪要、历史案例、人工复核结论，来源明确 | 可支持中置信推断 |
| C | 间接描述、二手转述、缺少上下文的片段 | 只能作为线索，不得单独支撑根因 |
| D | 无来源假设、模型自由推断 | 只能列为待验证项 |

## 引用格式

- 文件证据：`文件: <path>`。
- 行号证据：`文件: <path>:<line>`。
- 章节证据：`文件: <path>，章节: <heading>`。
- 冲突证据：同时列出支持与反证来源，并写明冲突点。

## 结论约束

1. 根因结论必须至少有一条 A 或 B 级证据。
2. 只有 C/D 级证据时，结论必须标记为“待验证”。
3. 概率判断必须说明依据来源；没有来源时概率字段保持为空。
4. 不得把“资料未显示异常”写成“确认无异常”，除非资料覆盖了对应验证范围。
```

Expected: file exists and defines A/B/C/D evidence levels.

- [ ] **Step 4: Verify skill files are discoverable**

Run:

```bash
find skills/custom/fault-zeroing -maxdepth 3 -type f | sort
```

Expected output includes:

```text
skills/custom/fault-zeroing/SKILL.md
skills/custom/fault-zeroing/references/evidence_rules.md
```

- [ ] **Step 5: Commit**

Run:

```bash
git add skills/custom/fault-zeroing/SKILL.md skills/custom/fault-zeroing/references/evidence_rules.md
git commit -m "feat(fault-zeroing): add custom skill"
```

Expected: commit succeeds.

---

### Task 2: Add output templates

**Files:**
- Create: `skills/custom/fault-zeroing/templates/zeroing_report.md`
- Create: `skills/custom/fault-zeroing/templates/fault_tree.json`
- Create: `skills/custom/fault-zeroing/templates/bottom_event_assessment.md`

- [ ] **Step 1: Write report template**

Create `skills/custom/fault-zeroing/templates/zeroing_report.md` with this content:

```markdown
# 归零报告

## 1. 问题概述

- 顶事件：
- 发生时间：
- 发生条件：
- 影响范围：
- 当前状态：

## 2. 输入资料

| 资料 | 类型 | 来源 | 覆盖范围 | 备注 |
| --- | --- | --- | --- | --- |

## 3. 故障现象

| 现象 | 证据来源 | 事实/推断 | 说明 |
| --- | --- | --- | --- |

## 4. 故障树分析

### 4.1 顶事件

### 4.2 中间事件

### 4.3 底事件

| ID | 底事件 | 说明 | 证据 | 状态 |
| --- | --- | --- | --- | --- |

## 5. 底事件概率评估

| 底事件 | 概率判断 | 置信度 | 依据 | 反证 | 验证状态 |
| --- | --- | --- | --- | --- | --- |

## 6. 根因归因

| 结论 | 证据等级 | 证据来源 | 置信度 | 说明 |
| --- | --- | --- | --- | --- |

## 7. 验证计划与结果

| 验证项 | 目的 | 方法 | 期望结果 | 当前结果 | 状态 |
| --- | --- | --- | --- | --- | --- |

## 8. 纠正措施

| 措施 | 对应原因 | 责任方 | 完成标准 | 状态 |
| --- | --- | --- | --- | --- |

## 9. 遗留风险

| 风险 | 原因 | 影响 | 建议动作 |
| --- | --- | --- | --- |

## 10. 附录：证据引用

| 编号 | 证据等级 | 来源 | 摘要 | 支撑结论 |
| --- | --- | --- | --- | --- |
```

Expected: template contains all ten required report sections.

- [ ] **Step 2: Write fault tree JSON template**

Create `skills/custom/fault-zeroing/templates/fault_tree.json` with this content:

```json
{
  "top_event": "",
  "intermediate_events": [],
  "bottom_events": [
    {
      "id": "",
      "name": "",
      "description": "",
      "evidence": [],
      "probability": null,
      "confidence": "low",
      "status": "to_verify"
    }
  ],
  "logic": []
}
```

Expected: file is valid JSON.

- [ ] **Step 3: Write bottom event assessment template**

Create `skills/custom/fault-zeroing/templates/bottom_event_assessment.md` with this content:

```markdown
# 底事件评估表

| 底事件 | 证据 | 概率判断 | 置信度 | 验证状态 | 后续动作 |
| --- | --- | --- | --- | --- | --- |
```

Expected: template contains the six assessment columns.

- [ ] **Step 4: Validate JSON template**

Run:

```bash
python3 -m json.tool skills/custom/fault-zeroing/templates/fault_tree.json
```

Expected: pretty-printed JSON is emitted with no parse error.

- [ ] **Step 5: Commit**

Run:

```bash
git add skills/custom/fault-zeroing/templates
git commit -m "feat(fault-zeroing): add output templates"
```

Expected: commit succeeds.

---

### Task 3: Add source-controlled custom agent and subagent templates

**Files:**
- Create: `docs/fault-zeroing-agent/agent/config.yaml`
- Create: `docs/fault-zeroing-agent/agent/SOUL.md`
- Create: `docs/fault-zeroing-agent/subagents.yaml`

- [ ] **Step 1: Create template directories**

Run:

```bash
mkdir -p docs/fault-zeroing-agent/agent
```

Expected: directory exists.

- [ ] **Step 2: Write custom agent config template**

Create `docs/fault-zeroing-agent/agent/config.yaml` with this content:

```yaml
name: fault-zeroing
description: 归零排故智能体，用于基于资料证据构建故障树、评估底事件并生成归零报告
tool_groups:
  - file:read
  - file:write
  - web
skills:
  - fault-zeroing
```

Expected: file uses only supported `AgentConfig` fields: `name`, `description`, `tool_groups`, `skills`.

- [ ] **Step 3: Write SOUL template**

Create `docs/fault-zeroing-agent/agent/SOUL.md` with this content:

```markdown
# Fault Zeroing Agent SOUL

## 身份

你是归零排故智能体，目标是基于用户提供的证据资料完成故障树构建、底事件评估、根因归因、验证计划和归零报告生成。

## 工作原则

1. 先读取用户输入、上传资料和目录结构，再形成分析结论。
2. 不得凭空给出底事件概率、根因判断或验证结果。
3. 每个关键结论必须绑定证据来源；证据不足时标记“待验证”。
4. 明确区分事实、推断、假设和验证建议。
5. 对冲突证据保留双方来源，不强行合并。
6. 输出报告前必须检查章节完整性和证据闭环。

## 输出要求

默认使用中文。最终输出必须包含 `fault_tree.json`、`bottom_event_assessment.md`、`zeroing_report.md`，并通过 `present_files` 展示。
```

Expected: SOUL explicitly forbids unsupported probability and root-cause claims.

- [ ] **Step 4: Write subagents config snippet**

Create `docs/fault-zeroing-agent/subagents.yaml` with this content:

```yaml
subagents:
  custom_agents:
    evidence-reader:
      description: 读取归零资料、抽取关键证据、标注文件和行号
      system_prompt: |
        你是资料读取子智能体。只负责读取资料、抽取证据、标注来源，不做根因结论。
        输出必须包含证据摘要、文件路径、行号或章节、证据等级和可能支撑的故障事件。
      tools:
        - glob
        - grep
        - read_file
      skills:
        - fault-zeroing
      model: inherit
      max_turns: 40
      timeout_seconds: 600
    fault-tree-builder:
      description: 根据顶事件和证据构建故障树
      system_prompt: |
        你是故障树构建子智能体。根据顶事件、故障现象和证据构建中间事件与底事件。
        不给出最终根因。证据不足的底事件必须标记为 to_verify。
      tools:
        - read_file
        - write_file
      skills:
        - fault-zeroing
      model: inherit
      max_turns: 40
      timeout_seconds: 600
    probability-assessor:
      description: 评估底事件概率、置信度和证据等级
      system_prompt: |
        你是底事件评估子智能体。基于证据强度、机理一致性、反证和复现实验情况评估底事件。
        没有明确依据时不得给出精确概率，必须保留 null 或定性判断。
      tools:
        - read_file
        - write_file
      skills:
        - fault-zeroing
      model: inherit
      max_turns: 40
      timeout_seconds: 600
    root-cause-analyst:
      description: 综合底事件概率和证据强度形成归因
      system_prompt: |
        你是根因归因子智能体。综合故障树、底事件评估和证据等级形成归因。
        根因结论必须绑定 A/B 级证据；只有 C/D 级证据时必须标记待验证。
      tools:
        - read_file
        - write_file
      skills:
        - fault-zeroing
      model: inherit
      max_turns: 40
      timeout_seconds: 600
    report-reviewer:
      description: 检查报告章节、证据引用和待验证项
      system_prompt: |
        你是报告审查子智能体。检查归零报告是否章节完整、证据闭环、待验证项清晰。
        只输出问题清单和修订建议，不新增未被证据支持的技术结论。
      tools:
        - read_file
        - write_file
      skills:
        - fault-zeroing
      model: inherit
      max_turns: 30
      timeout_seconds: 600
```

Expected: each custom subagent omits `task`, `ask_clarification`, and `present_files` from its tool whitelist.

- [ ] **Step 5: Validate YAML files**

Run:

```bash
python3 -c "import yaml; [yaml.safe_load(open(p, encoding='utf-8')) for p in ['docs/fault-zeroing-agent/agent/config.yaml','docs/fault-zeroing-agent/subagents.yaml']]"
```

Expected: command exits successfully.

- [ ] **Step 6: Commit**

Run:

```bash
git add docs/fault-zeroing-agent/agent docs/fault-zeroing-agent/subagents.yaml
git commit -m "feat(fault-zeroing): add agent config templates"
```

Expected: commit succeeds.

---

### Task 4: Add sample evidence packages

**Files:**
- Create: `docs/fault-zeroing-agent/samples/case-01/problem_statement.md`
- Create: `docs/fault-zeroing-agent/samples/case-01/logs/system_log.md`
- Create: `docs/fault-zeroing-agent/samples/case-01/test_records/reproduction_record.md`
- Create: `docs/fault-zeroing-agent/samples/case-01/design_docs/interface_constraints.md`
- Create: `docs/fault-zeroing-agent/samples/case-02/problem_statement.md`
- Create: `docs/fault-zeroing-agent/samples/case-02/logs/system_log.md`
- Create: `docs/fault-zeroing-agent/samples/case-02/test_records/reproduction_record.md`
- Create: `docs/fault-zeroing-agent/samples/case-02/historical_cases/similar_case.md`

- [ ] **Step 1: Create sample directories**

Run:

```bash
mkdir -p docs/fault-zeroing-agent/samples/case-01/logs docs/fault-zeroing-agent/samples/case-01/test_records docs/fault-zeroing-agent/samples/case-01/design_docs docs/fault-zeroing-agent/samples/case-02/logs docs/fault-zeroing-agent/samples/case-02/test_records docs/fault-zeroing-agent/samples/case-02/historical_cases
```

Expected: all sample directories exist.

- [ ] **Step 2: Write case 01 problem statement**

Create `docs/fault-zeroing-agent/samples/case-01/problem_statement.md`:

```markdown
# Case 01 问题描述

顶事件：地面联试期间，控制单元在高负载遥测回放时发生一次异常复位。

发生条件：连续运行 43 分钟后，遥测回放速率从 1x 提升到 4x，约 90 秒后出现复位。

影响范围：控制单元复位导致当前测试流程中断，未发现外设永久损坏。

已知约束：本样例为脱敏 PoC 资料，用于验证智能体能否基于证据形成故障树和待验证项。
```

Expected: file states the top event.

- [ ] **Step 3: Write case 01 supporting files**

Create `docs/fault-zeroing-agent/samples/case-01/logs/system_log.md`:

```markdown
# 系统日志摘录

| 时间 | 事件 | 备注 |
| --- | --- | --- |
| T+00:41:30 | telemetry_replay_rate=4x | 操作员提升回放速率 |
| T+00:42:58 | cpu_load=92% | 高负载持续 70 秒 |
| T+00:43:02 | watchdog_warning | 看门狗喂狗间隔超过阈值 |
| T+00:43:05 | unit_reset | 控制单元复位 |
| T+00:43:40 | reboot_complete | 复位后恢复 |
```

Create `docs/fault-zeroing-agent/samples/case-01/test_records/reproduction_record.md`:

```markdown
# 复现实验记录

| 轮次 | 条件 | 结果 |
| --- | --- | --- |
| 1 | 1x 回放，运行 60 分钟 | 未复现 |
| 2 | 4x 回放，运行 20 分钟 | 未复现 |
| 3 | 4x 回放 + 诊断日志全量开启 | 运行 18 分钟后出现 watchdog_warning，未复位 |
| 4 | 4x 回放 + CPU 压测 | 运行 11 分钟后复位 |
```

Create `docs/fault-zeroing-agent/samples/case-01/design_docs/interface_constraints.md`:

```markdown
# 接口约束

- 看门狗喂狗周期设计值为 100 ms。
- 遥测回放服务与诊断日志服务共享同一 CPU 核。
- 当 CPU 负载持续超过 90% 时，低优先级诊断日志允许丢弃，不应阻塞喂狗线程。
```

Expected: files provide at least one A-level or B-level source for watchdog and CPU-load hypotheses.

- [ ] **Step 4: Write case 02 files**

Create `docs/fault-zeroing-agent/samples/case-02/problem_statement.md`:

```markdown
# Case 02 问题描述

顶事件：环境试验后，数据采集链路出现间歇性丢帧。

发生条件：低温循环恢复常温后，采集链路在 10 分钟内出现 3 次短时丢帧。

影响范围：采集数据不连续，主控流程未中断。
```

Create `docs/fault-zeroing-agent/samples/case-02/logs/system_log.md`:

```markdown
# 链路日志摘录

| 时间 | 事件 | 备注 |
| --- | --- | --- |
| T+00:02:15 | frame_drop=12 | 首次丢帧 |
| T+00:04:44 | link_retry=3 | 链路重试 |
| T+00:07:31 | frame_drop=8 | 第二次丢帧 |
| T+00:09:52 | temperature=24C | 常温稳定 |
```

Create `docs/fault-zeroing-agent/samples/case-02/test_records/reproduction_record.md`:

```markdown
# 复现实验记录

| 轮次 | 条件 | 结果 |
| --- | --- | --- |
| 1 | 常温直接启动 | 未复现 |
| 2 | 低温循环后恢复常温 | 复现 1 次丢帧 |
| 3 | 低温循环后重新插拔连接器 | 未复现 |
```

Create `docs/fault-zeroing-agent/samples/case-02/historical_cases/similar_case.md`:

```markdown
# 历史相似案例

一次历史试验中，连接器接触裕度不足导致低温循环后链路重试增加。该案例最终通过复测连接器接触电阻确认，但当前样例尚无接触电阻数据。
```

Expected: case 02 includes both supporting evidence and an explicit missing verification item.

- [ ] **Step 5: Commit**

Run:

```bash
git add docs/fault-zeroing-agent/samples
git commit -m "test(fault-zeroing): add sample evidence packages"
```

Expected: commit succeeds.

---

### Task 5: Add validation record and manual run instructions

**Files:**
- Create: `docs/fault-zeroing-agent/验证记录.md`

- [ ] **Step 1: Write validation document**

Create `docs/fault-zeroing-agent/验证记录.md` with this content:

```markdown
# 归零排故智能体 PoC 验证记录

## 安装准备

1. 将 `docs/fault-zeroing-agent/agent/config.yaml` 和 `docs/fault-zeroing-agent/agent/SOUL.md` 安装到当前用户的 iDeer custom agent 目录。
2. 将 `docs/fault-zeroing-agent/subagents.yaml` 中的 `subagents` 配置合并到本地 `config.yaml`。
3. 确认 `skills/custom/fault-zeroing/SKILL.md` 存在。
4. 启动 iDeer 后选择 `fault-zeroing` 智能体。

## 推荐测试提示词

```text
请基于我上传的资料完成归零排故分析。先读取问题描述、日志、试验记录和设计约束，构建故障树，评估底事件，生成 fault_tree.json、bottom_event_assessment.md 和 zeroing_report.md，并展示所有输出文件。
```

## 验收标准

| 标准 | 通过要求 | Case 01 | Case 02 |
| --- | --- | --- | --- |
| 输出 fault_tree.json | 文件存在，包含顶事件、底事件、logic | 未执行 | 未执行 |
| 输出 bottom_event_assessment.md | 文件存在，底事件有证据和置信度 | 未执行 | 未执行 |
| 输出 zeroing_report.md | 文件存在，包含十个章节 | 未执行 | 未执行 |
| 证据引用 | 关键结论绑定资料来源 | 未执行 | 未执行 |
| 待验证项 | 证据不足项明确标记待验证 | 未执行 | 未执行 |
| 子智能体分工 | 运行记录能看到资料读取、故障树、概率评估、归因、审查职责 | 未执行 | 未执行 |

## 问题记录

| 日期 | 样例 | 问题 | 影响 | 后续动作 |
| --- | --- | --- | --- | --- |
```

Expected: document contains setup, prompt, acceptance table, and issue table.

- [ ] **Step 2: Verify all planned files exist**

Run:

```bash
find skills/custom/fault-zeroing docs/fault-zeroing-agent -type f | sort
```

Expected: output includes all files listed in this plan's File Structure table.

- [ ] **Step 3: Validate JSON and YAML again**

Run:

```bash
python3 -m json.tool skills/custom/fault-zeroing/templates/fault_tree.json
python3 -c "import yaml; [yaml.safe_load(open(p, encoding='utf-8')) for p in ['docs/fault-zeroing-agent/agent/config.yaml','docs/fault-zeroing-agent/subagents.yaml']]"
```

Expected: both commands exit successfully.

- [ ] **Step 4: Detect code graph impact before final commit**

Run GitNexus change detection:

```text
gitnexus_detect_changes(scope="all", repo="ideer")
```

Expected: changed symbols list is empty or limited to non-code documentation/skill artifacts. If Python or TypeScript symbols appear unexpectedly, stop and investigate before committing.

- [ ] **Step 5: Commit**

Run:

```bash
git add docs/fault-zeroing-agent/验证记录.md
git commit -m "docs(fault-zeroing): add poc validation checklist"
```

Expected: commit succeeds.

---

## Execution Notes

- This PoC intentionally does not add a formal RAG/vector knowledge base.
- This PoC intentionally does not add dedicated front-end pages.
- This PoC intentionally does not edit FastAPI, LangGraph, Next.js, or runtime loader code.
- Runtime installation of the custom agent is per-user. Use the source templates in `docs/fault-zeroing-agent/agent/` as the canonical files, then copy them into the target user's iDeer agent directory during manual validation.
- If a later task requires automating runtime installation, add a separate plan task for a small script and tests rather than expanding this file/template task.

## Self-Review

Spec coverage:

- Custom agent config: Task 3.
- SOUL behavior guardrails: Task 3.
- Custom skill: Task 1.
- Report, fault-tree, and bottom-event templates: Task 2.
- Five custom subagents: Task 3.
- Two sample evidence packages: Task 4.
- End-to-end validation record: Task 5.

Known gaps:

- This plan does not automate copying agent templates into `backend/.ideer/users/{user_id}/agents/fault-zeroing/`.
- This plan does not execute the PoC through a live iDeer chat session because that requires selecting the runtime user, local config, model availability, and server mode.
