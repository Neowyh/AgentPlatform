# 归零排故智能体 PoC 验证记录

## 安装准备

1. 将 `docs/fault-zeroing-agent/agent/config.yaml` 和 `docs/fault-zeroing-agent/agent/SOUL.md` 安装到当前用户的 iDeer custom agent 目录。
2. 运行 `python scripts/seed_fault_zeroing_workflow.py` 将 `workflows/fault-zeroing.yaml` 入库（幂等，重复执行会跳过已入库版本）。`docs/fault-zeroing-agent/subagents.yaml` 已废弃（`deprecated`），安装脚本不再引用。
3. 确认 `skills/custom/fault-zeroing/SKILL.md` 存在。
4. 启动 iDeer 后选择 `fault-zeroing` 智能体并触发对应工作流。

## 推荐测试提示词

```text
请基于我上传的资料完成归零排故分析。先读取问题描述、日志、试验记录和设计约束，构建故障树，评估底事件，生成 fault_tree.json、fault_tree.svg、bottom_event_assessment.md、analysis_process.svg 和 zeroing_report.md，并展示所有输出文件。
```

## 验收标准

| 标准 | 通过要求 | Case 01 | Case 02 |
| --- | --- | --- | --- |
| 输出 fault_tree.json | 文件存在，包含顶事件、底事件、logic | 未执行 | 未执行 |
| 输出 fault_tree.svg | 文件存在，能展示故障树结构、逻辑门和底事件状态 | 未执行 | 未执行 |
| 输出 bottom_event_assessment.md | 文件存在，底事件有证据和置信度 | 未执行 | 未执行 |
| 输出 analysis_process.svg | 文件存在，能展示证据提取到文档生产的分析链路 | 未执行 | 未执行 |
| 输出 zeroing_report.md | 文件存在，包含十个章节 | 未执行 | 未执行 |
| 证据引用 | 关键结论绑定资料来源 | 未执行 | 未执行 |
| 待验证项 | 证据不足项明确标记待验证 | 未执行 | 未执行 |
| 工作流节点链路 | 运行记录能看到证据提取、故障树构建、底事件评估、根因归因、纠正措施、文档生产节点顺序执行 | 未执行 | 未执行 |
| 职责边界 | 报告包含阶段职责说明（演绎建树不依赖证据台账、证据检漏只做添加不做删除、文档阶段不修改分析数据） | 未执行 | 未执行 |

## 自动化测试记录（工作流引擎重构）

| 日期 | 范围 | 结果 |
| --- | --- | --- |
| 2026-08-03 | 工作流 YAML 解析（`test_v2_parser_accepts_fault_zeroing_workflow`） | 通过 |
| 2026-08-03 | 引擎 system_prompt 覆盖（`test_v2_agent_adapter.py` 5 用例：SOUL+override / 仅 SOUL / 仅 override / agent 缺失 / executor 失败） | 通过 |
| 2026-08-03 | 9 节点全链路集成（`test_fault_zeroing_runtime.py`：产物与 state、执行顺序、重试 attempts==2） | 通过 |
| 2026-08-03 | 纠正措施 schema 与验证器（`test_validate_fault_zeroing_outputs.py` 34 用例） | 通过 |
| 2026-08-03 | 安装脚本 stdlib 约束、seed 幂等入库、内网离线打包（install 12 / seed 5 / intranet 15 用例） | 通过 |
| 2026-08-03 | SOUL/SKILL 精简后输出要求锁定（`test_fault_zeroing_visual_outputs.py`） | 通过 |

## 问题记录

| 日期 | 样例 | 问题 | 影响 | 后续动作 |
| --- | --- | --- | --- | --- |
