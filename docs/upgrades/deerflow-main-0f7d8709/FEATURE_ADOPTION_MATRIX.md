# Feature adoption matrix

| Area | Upstream SHA | Current status | Target owner / seam | Evidence | Blocking closure |
|---|---|---|---|---|---|
| Runtime / context | locked `0f7d8709` | **Legacy `ideer` harness deleted (commit `19a758cb`)**; `import deerflow` succeeds, `import ideer` fails, `backend/app` direct ideer imports are 0. Gateway startup uses DeerFlow AppConfig and shared session factory, canonical auth repository uses DeerFlow `UserRow`, AgentPlatform owns RBAC and visibility/audit models, Extension host loads configured plugins, mounts contributed routers after host routes, projects caller-only principals, binds dynamic Run Evidence snapshots to worker context, and resource/auth/audit path resolution uses DeerFlow modules. Canonical Agents assemble through DeerFlow: `assemble_lead_agent(frozen=...)` (PATCH-010) consumes the server-frozen UUID/version/hash closure. The run-frozen skill view mount is restored through the neutral `RUN_SKILL_VIEW_RESOLVER` hook (PATCH-012) with the enterprise resolver installed in gateway and worker | DeerFlow runtime; AgentPlatform extension for enterprise context | Runtime Foundation focused evidence, resource path regression (1), AuthConfig suite (6), audit suite (12), auth repository suite (17), RBAC/reset suite (34), visibility mapper boundary (2), config migration suite (12), workflow seam boundary (4), frozen-input suite (4), adapter suite (2), lead-agent/assembly/prompt suites (110), and adapter/router regressions in `BASELINE_REPORT.md` | Control-plane caller migration and production `ideer` import removal: done (commit `19a758cb`). Remaining: stale unit-test triage in gateway/sandbox/runtime/tools, full `pr-standard`/`core-full` lanes, and E2E convergence |
| Memory | locked `0f7d8709` | Gateway CRUD and config response now use the DeerFlow `MemoryManager`/schema through the AgentPlatform adapter; config-upgrade migrates legacy top-level Memory fields into directory-backed `backend_config` even for current-version profiles | DeerFlow `MemoryManager`; AgentPlatform caller/agent scope adapter | Adapter/router/config: 29 focused passes; isolation/migration: 26; storage/restart: 38; queue: 32; Markdown migration: 5; config-version suite 11 passed | Full memory acceptance and end-to-end restart evidence remain open |
| Sub-agents | locked `0f7d8709` | Receipt and delegation contract is green; runtime-stamped tool receipts and sub-agent verification now feed the shared Run Evidence Envelope when the AgentPlatform extension is active, with missing receipts marked `UNVERIFIED` | DeerFlow delegation runtime; AgentPlatform policy/visibility | Receipt middleware/task-tool/projection suites 32 passed; see `SUBAGENT_ACCEPTANCE_REPORT.md` | Real receipt verification in a complete initialized-DB workflow run |
| Authz | locked `0f7d8709` | Assembly/runtime deny paths and trusted Gateway caller projection are green; caller-safe shared-resource evidence now rejects owner credential/memory projection; full run-config compatibility remains incomplete | DeerFlow AuthorizationProvider + AgentPlatform policy extension | Authorization/tool-filter focused paths 15 + 17 passed; extension boundary and caller-safe evidence tests pass; see `AUTHORIZATION_ACCEPTANCE_REPORT.md` | End-to-end shared-resource run with caller credential, memory and tool-permission isolation; complete Gateway config compatibility |
| Skills | locked `0f7d8709` | Enabled-only defaults and canonical Run views now use the single managed `/mnt/skills` mount; no `/mnt/run-skills` parallel namespace remains | Resource Resolver → DeerFlow managed `/mnt/skills` projection | Canonical Run and three-way sandbox projection suites 26 passed; frozen version/hash and read-only mount checks covered | Full initialized-DB run projection and no-competing-mount acceptance in standard lane |
| Resources / workflows | locked `0f7d8709` | `/api/resources` remains canonical; ResourceMetadata and Resource Catalog model families now live in the AgentPlatform model module and use the shared DeerFlow session boundary; resource-runtime consumers now use AgentPlatform Resource/RBAC models; Workflow V2 model family is registered in DeerFlow and Gateway/Worker/Store/RunRecord/resource governance use that namespace; the duplicate `ideer.persistence.models.workflow_v2` implementation has been deleted; canonical Workflow Run snapshots now persist and bind caller-scoped Run Evidence, surviving mutable recovery updates; Code Evidence acceptance is AgentPlatform-owned with a DeerFlow runtime path projection and DeerFlow scanner implementation; workflow DB integration incomplete | AgentPlatform control plane; DeerFlow runtime observer | ResourceMetadata store suite 7 passed; Resource API collection 19; Workflow table-contract and combined metadata checks 2 passed; Run Evidence/WorkflowV2Store/runtime-config focused suite 28 passed; canonical Agent adapter suite 17 passed; Code Evidence/path suite 13 passed; scanner suite 5 passed; SRS smoke passed; fault-zeroing harness guard suite 8 passed but live case incomplete; resource governance execution incomplete in sandbox | Snapshot freeze, workflow receipts and initialized DB integration suite; complete live fault-zeroing and full database migration convergence |
| Database migrations | locked `0f7d8709` | DeerFlow and AgentPlatform retain independent forward-only histories; DeerFlow now uses `deerflow_alembic_version`, with numeric legacy-row adoption and no interpretation of control-plane heads | AgentPlatform control plane + DeerFlow runtime migration owners | Version-table isolation/adoption 3 passed; heads recorded in migration acceptance report | Existing DB and PostgreSQL fresh/existing migration acceptance; broader bootstrap suite incomplete |
| Frontend resource boundary | locked `0f7d8709` | Agent UI list/detail/create/update/archive/import/export/favorite/name checks now use UUID-backed `/api/resources`; legacy `/api/agents` is no longer a canonical source | Resource Center facade; DeerFlow Agent runtime | Canonical Agent API contract tests are present; local pnpm runner unavailable in this sandbox | Run frontend Agent contract tests and full frontend standard lane |
| Offline / intranet | locked `0f7d8709` | Default-deny network and DeerFlow runtime paths configured; Compose gateway readiness and local launcher port contracts are enforced | AgentPlatform deployment overlay | 13 network/config tests; gateway startup/deploy focused lane 14 passed; offline pre-check reports missing host Compose/images/env only | Docker Compose/images, bundle build and fresh air-gapped install |
